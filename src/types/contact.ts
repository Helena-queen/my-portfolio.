export type ContactFormData = {
  name: string;
  email: string;
  subject: string;
  message: string;
}

export type ContactItemData = {
  title: string;
  value: string;
}

export type SocialLink = {
  name: string;
  url: string;
}